const template = `
.chat__footer
    .new-message
        .new-message__attachment
            !=attachIcon
        .new-message__input
            !=newMessageInput
            !=sendMessageButton
`;

export default template;
